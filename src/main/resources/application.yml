spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  thymeleaf:
    cache: false

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update

  jwt:
    secret: ${JWT_SECRET}
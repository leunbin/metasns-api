spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    database: h2
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  data:
    redis:
      host: localhost
      port: 6379

jwt:
  secret: cB2kEC8YoIgACzW75Uw63TxJWzk5fLgBbcW98q0H4jM=



storage:
  endpoint: testEndpoint
  path: testPath
  access-key: testAccessKey
  secret-key: testSecretKey
  bucket: testBucket
  image-dir: testImageDir

